doctype html
html(lang="en")
  head
    title= pageTitle
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    link(rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/normalize/8.0.1/normalize.min.css")
    style.
      body {
        font-family: Arial, sans-serif;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
      }
      .login-container {
        text-align: center;
      }
      .login-button {
        padding: 10px 20px;
        font-size: 16px;
        cursor: pointer;
      }
    script(type="module").
      import { signInWithGoogle } from './../public/js/googleAuth.js';

      function googleLogin() {
        signInWithGoogle();
      }
  body
    .login-container
      h1= pageTitle
      a.login-button(href="/googlesignin") Login
